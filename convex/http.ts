/**
 * Convex HTTP Routes
 * 
 * HTTP endpoints for the Reveal application.
 * Authentication has been removed.
 */

import { httpRouter } from "convex/server";

const http = httpRouter();

// Add custom HTTP routes here if needed

export default http;

